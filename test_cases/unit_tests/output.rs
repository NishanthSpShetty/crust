fn not_main(p: i32, q: char, r: i64) -> i32 {
    let p: i32;
    let q: i32;
    let a: i32;
    let b: i32;
    let c: i32;
    c = 100;
    b = c;
    a = b;
}
fn main() {
    let mut argv = env::args();
    let mut argc = argv.len();
}
